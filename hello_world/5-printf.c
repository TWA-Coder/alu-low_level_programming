#include <stdio.h>
/**
*main - will print the given input to the console
*
*Return: 0 if the program runs successfully
*/

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
